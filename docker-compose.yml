name: gastos-whatsapp

services:
  api:
    build: .
    container_name: gastos-whatsapp-api
    restart: unless-stopped
    ports:
      - "127.0.0.1:8010:8000"
    volumes:
      - ./data:/app/data
    mem_limit: 256m
    cpus: "0.50"
